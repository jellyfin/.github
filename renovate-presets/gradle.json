{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Default preset for use with Jellyfin's JVM repos",
  "extends": ["github>jellyfin/.github//renovate-presets"],
  "packageRules": [
    {
      "description": "Add the java GitHub label to Gradle dependency bump PRs",
      "matchManagers": ["gradle"],
      "labels": ["java"]
    },
    {
      "description": "Ensure Gradle Wrapper bump PRs are disabled",
      "matchDepTypes": ["gradle-wrapper"],
      "enabled": false
    }
  ]
}
